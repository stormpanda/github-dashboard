# GitHub API Token (Optional if 'gh' CLI is already authenticated locally)
GITHUB_TOKEN=your_github_personal_access_token_here

# Comma-separated list of repositories to track in the format: owner/repo
GITHUB_REPOS=owner/repo1,owner/repo2
